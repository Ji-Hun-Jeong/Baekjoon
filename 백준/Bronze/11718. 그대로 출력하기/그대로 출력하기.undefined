#include <iostream>
#include <string>
#include <print>

int main()
{
	std::ios::sync_with_stdio(false);
	std::cin.tie(nullptr);
	std::cout.tie(nullptr);

	std::string S;
	while (std::getline(std::cin, S))
		std::cout << S << '\n';
}